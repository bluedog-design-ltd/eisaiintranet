{% extends "block--bare.html.twig" %}
{#
/**
 * @file
 * Theme override for local actions (primary admin actions.)
 *
 * @ingroup templates
 */
#}
{% block content %}
  {% if content %}
	<div class="container local-actions">
     <div class="row">
    <ul class="list-inline local-actions">{{ content }}</ul>
	</div>
     </div>
  {% endif %}
{% endblock %}
